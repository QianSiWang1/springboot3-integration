server:
  port: 8881
  servlet:
    context-path: /boot-eureka-client-one
spring:
  application:
    name: boot-eureka-client-one
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  server:
    waitTimeInMsWhenSyncEmpty: 0
management:
  endpoints:
    web:
      exposure:
        include: "*"